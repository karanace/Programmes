#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;
public Class Boxing
{
    int count[5],exhausted[5];
    public int maxCredit(vector<int> judge1(50),vector<int> judge2(50),vector<int> judge3(50),vector<int> judge4(50),vector<int> judge5(50))
    {
        int indexJudge[5],min;
        
        
        while(numExhausted() <3)
        {
                        
        }
    } 
int numExhausted()
{
    return exhausted[0] + exhausted[1] + exhausted[2]+ exhausted[3]+ exhausted[4];
}    

}
